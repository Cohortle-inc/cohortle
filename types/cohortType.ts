export interface CohortType {
  name: string;
  url?: string;
  description: string;
  goal?: string;
  referral?: string;
  community_structure?: string;
  start_date: string;
  end_date: string;
  max_members: number;
}
